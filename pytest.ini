[pytest]
# Sin esto, "from src..." solo funciona si se invoca como "python -m pytest"
# desde la raiz del repo; con esto funciona tambien con el comando "pytest"
# a secas (que es lo que usa el workflow de CI).
pythonpath = .
