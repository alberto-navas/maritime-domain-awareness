pandas>=2.2.0        # manipulacion tabular y agrupacion temporal por MMSI
matplotlib>=3.8.0      # mapa estatico de trazas con los hallazgos marcados
pyyaml>=6.0.1            # carga de umbrales de configuracion desde YAML
shapely>=2.0.0             # geometria/point-in-polygon para las zonas portuarias
folium>=0.16.0               # mapa animado (Leaflet) con TimestampedGeoJson para el panel web
fastapi>=0.110.0               # interfaz web (subir CSV -> ver mapa animado + hallazgos en el navegador)
uvicorn>=0.29.0                  # servidor ASGI para correr la interfaz web
jinja2>=3.1.0                      # plantillas de la interfaz web
python-multipart>=0.0.9              # soporte de subida de archivos (multipart/form-data) en FastAPI
pytest>=8.0.0              # framework de tests (dependencia de desarrollo)
pytest-cov>=5.0.0            # cobertura de tests (dependencia de desarrollo)
httpx>=0.27.0                  # cliente HTTP usado por TestClient en los tests de la web (dependencia de desarrollo)
ruff>=0.16.0                     # lint + formato de codigo (dependencia de desarrollo)
mypy>=1.10.0                       # comprobacion estatica de tipos (dependencia de desarrollo)
